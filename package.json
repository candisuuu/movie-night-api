{
	"private": true,
	"version": "1.1.0",
	"scripts": {
		"deploy": "wrangler publish src/index.js",
		"dev": "wrangler dev src/index.js --local",
		"test": "vitest",
		"start-stackblitz": "WRANGLER_SEND_METRICS=false wrangler dev src/index.js --local"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^3.0.0",
		"vitest": "^0.24.5",
		"wrangler": "^2.12.0"
	},
	"dependencies": {
		"@tsndr/cloudflare-worker-jwt": "^2.2.1",
		"itty-router": "^3.0.11"
	}
}
